package com.example.model;

public enum UserState {
    AWAITING_USERNAME,
    AWAITING_PASSWORD,
    AWAITING_EMAIL,
    AWAITING_PHONE,
    COMPLETED,
    AWAITING_LOGIN_USERNAME,  // Add this
    AWAITING_LOGIN_PASSWORD,   // Add this
  
}
